# Ansible Layout

Use Ansible for host-level operations:

- package installation
- config file changes
- service restarts
- patching steps
- disk growth runbooks when needed

Suggested folders:

- `inventories/`
- `playbooks/`
- `roles/`
- `group_vars/`

